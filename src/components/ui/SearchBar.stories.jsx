import SearchBar from './SearchBar';

const meta = {
  title: 'UI/SearchBar',
  component: SearchBar,
  argTypes: {},
  tags: ['autodocs'],
};

export default meta;

export const Default = {
  args: {},
};
